import 'package:flutter/material.dart';

class ListMarkMovie extends StatelessWidget {
  const ListMarkMovie({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
